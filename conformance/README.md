# HMX conformance fixtures — PLACEHOLDER

The conformance suite (a deterministic Python generator, committed golden JSON
snapshots, and the valid/ + invalid/ vectors) is **built in step A11**. A1 ships
only the empty `valid/` and `invalid/` directories.

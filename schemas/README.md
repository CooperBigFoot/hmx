# HMX JSON Schemas — PLACEHOLDER

The committed JSON schemas (`manifest.schema.json`, the field-registry and
mapping/domain schemas, `describe.schema.json`, `validate.schema.json`) are
**authored in step A2**. This directory ships empty in the A1 skeleton apart from
this note.

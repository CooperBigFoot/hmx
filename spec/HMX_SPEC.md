# HMX Specification — PLACEHOLDER

> **Status: skeleton (M13 / step A1).** The normative HMX (Hydrology Model
> Exchange) specification is **authored in step A2**, not here. This file exists
> only so the `spec/` directory is present in the A1 workspace skeleton.
>
> Nothing in this file is normative. A2 replaces it with the RFC-2119 numbered
> sections covering: the manifest, the typed field registry, cross-domain mappings
> + multi-entity domains + derivable entity cardinality, the manifest content-hash,
> CRS-as-explicit-per-package, and the `describe` / `validate` output wire shapes.
